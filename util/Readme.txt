vsskill   - Kills vss processes owned by another (unix) user.
vssping   - Verify that vss is running.
vssflush  - Empties vss's memory and kills its active sounds.
audtest   - Run a single .aud file, then type in messagegroup names
	    (with no parameters).
